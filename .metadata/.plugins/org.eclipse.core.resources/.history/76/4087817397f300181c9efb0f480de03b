package com.stream;

import java.awt.List;
import java.util.Arrays;

public class IntermediateOperations {
	List number= (List) Arrays.asList(2,3,4,5);
	
	List square=number.stream().map(x->x*x).collect(Collectors.toList());
	
}
